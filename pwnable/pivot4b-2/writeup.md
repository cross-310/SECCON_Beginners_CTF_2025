`read()`に0x10バイトのオーバーフローあり。
ASLRが有効なので、素直にret2vulnなどはできない。
しかし、リターンアドレスの下位1バイトを書き換えることでASLRの影響を受けずにret2vulnできる。
またこのとき、printfによってリターンアドレスを得られるので、elf baseを得られる。

ここで、vulnでretが呼ばれるときにrdiがlibcのシンボルを指していることに気がつく。
そこで、vulnのputs関数の直前へリターンすることで、libc base leakできる。

最後に、one_gadgetへ飛んであげることでシェルを得られる。
